# for each firmware build in "Build-Release.ps1" such an install script has to be delivered.
# the name must start with "Upload-Firmware"
# <firmware-name> is the firmware-name you used in Build-Step.ps1 during Build-Release.ps1
./data/Upload-Firmware-Generic-RP2040.ps1 SoundModule.uf2
